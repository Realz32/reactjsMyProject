const Saludo = (props) => {
    return(
        <h1>
            
            Hola {props.name}!    
            Tienes {props.edad} años;
            
            </h1>
    )

}

export default Saludo